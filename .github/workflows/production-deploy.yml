name: Production Deployment

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  NODE_VERSION: '18'
  PRODUCTION_SERVER: ${{ secrets.PRODUCTION_SERVER }}

jobs:
  # test:
  #   name: Run Tests
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci

  #     - name: Run tests
  #       run: npm test -- --watchAll=false --coverage

  #     - name: Upload coverage reports
  #       uses: codecov/codecov-action@v3
  #       if: always()
  #       with:
  #         files: ./coverage/lcov.info
  #         fail_ci_if_error: false

  build:
    name: Build Application
    # needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build production bundle
        run: npm run build
        env:
          CI: true
          GENERATE_SOURCEMAP: false

      # - name: Create build artifact
      #   run: |
      #     cd build
      #     tar -czf ../build-${{ github.sha }}.tar.gz .
      #     cd ..

      # - name: Upload build artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: production-build
      #     path: build-${{ github.sha }}.tar.gz
      #     retention-days: 30

  # deploy:
  #   name: Deploy to Production
  #   needs: build
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: production
  #     url: https://${{ secrets.PRODUCTION_DOMAIN }}
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Download build artifact
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: production-build

  #     - name: Setup Python for Ansible
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.11'

  #     - name: Install Ansible
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install ansible

  #     - name: Setup SSH key
  #       run: |
  #         mkdir -p ~/.ssh
  #         echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
  #         chmod 600 ~/.ssh/deploy_key
  #         ssh-keyscan -H ${{ secrets.PRODUCTION_SERVER }} >> ~/.ssh/known_hosts

  #     - name: Run Ansible deployment
  #       run: |
  #         cd ansible
  #         ansible-playbook -i inventory/production.yml \
  #           deploy.yml \
  #           --private-key ~/.ssh/deploy_key \
  #           --extra-vars "build_artifact=../build-${{ github.sha }}.tar.gz" \
  #           --extra-vars "app_version=${{ github.sha }}"
  #       env:
  #         ANSIBLE_HOST_KEY_CHECKING: False

  #     - name: Cleanup SSH key
  #       if: always()
  #       run: rm -f ~/.ssh/deploy_key

  # verify:
  #   name: Verify Deployment
  #   needs: deploy
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Health check
  #       run: |
  #         response=$(curl -s -o /dev/null -w "%{http_code}" https://${{ secrets.PRODUCTION_DOMAIN }})
  #         if [ $response -eq 200 ]; then
  #           echo "Deployment successful! Application is responding with HTTP 200"
  #         else
  #           echo "Deployment verification failed! HTTP status: $response"
  #           exit 1
  #         fi

  #     - name: Notify deployment success
  #       if: success()
  #       run: echo "Production deployment completed successfully!"

  #     - name: Notify deployment failure
  #       if: failure()
  #       run: echo "Production deployment failed! Please check the logs."
